{
  "name": "lambda01",
  "version": "1.0.0",
  "description": "",
  "main": "hello-world.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./src/hello-world.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/hello-world.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.121",
    "esbuild": "^0.19.3",
    "typescript": "^5.2.2"
  }
}
